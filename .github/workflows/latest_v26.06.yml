name: Latest release on CLN v26.06.x

on:
    release:
        types: [published, edited]
    workflow_dispatch:

jobs:
    call-ci:
        uses: ./.github/workflows/ci.yml
        with:
            cln-version: "v26.06"
            pyln-version: "26.6"
            tagged-release: true
